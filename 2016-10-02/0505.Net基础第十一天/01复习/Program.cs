using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace _01复习
{
    class Program
    {
        static void Main(string[] args)
        {
            /*
             *字符串
             * 1、字符串的不可变性---每次给它赋值的时候都会在内存中新建一个空间
             * 2、字符串的一坨方法
             * Split()           分割(把字符串里不想要的给割掉)返回一个字符串类型的数组
             * Substring()       切割
             * ToUpper()         转大写
             * ToLower()         转小写
             * Trim()            去两端的空格
             * TrimEnd()         去后面的
             * TrimStart()       去前面的
             * Contains()        判断是否包含（记住）
             * Replace()         替换（把老的字符串字符替换成新的、、、）
             * Indexof           查找某一个字符第一次出现的位置 没有的话 返回-1
             * LastIndexof                     最后
             * StartsWith        以、、、开始
             * EndWith
             * string.join()     把一个数组按照指定的分割符链接
             * string.IsNullOrEmpty()   是否为null或者为空
             * ToCharArray()            变成一个字符串类型的数组
             * new string(char[] chs)   返回一个字符串
             * 
             * 
             * 继承：解决类中的代码冗余
             * 属性、方法、没有继承私有字段。（子类对象能狗.出来的就是继承来的）。构造函数、没有继承构造函数
             * 子类默认调用的是父类无参数的构造函数。为什么要调用？因为子类要使用父类的成员，所以说要创建父类对象
             * 
             * 一、this:
             * 1、当前类的对象
             * 2、调用自己的构造函数
             * 二、base:调用父类的构造函数
             * 三、new:
             * 1、创建对象
             * 2、隐藏父类成员
             */
        }
    }
}
