<Window x:Class="多窗口案例.InputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:多窗口案例"
        mc:Ignorable="d"
        Title="请输入值" Height="150" Width="300" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <TextBox Name="txtValue" Grid.ColumnSpan="2"></TextBox>
            <Button Name="btnOK" Margin="5" Content="确定" Grid.Row="1" Click="btnOK_Click"></Button>
            <Button Name="btnCancel" Margin="146,5,11,5" Content="取消" Grid.Row="1" Click="btnCancel_Click" Grid.ColumnSpan="2"/>
        </Grid>
    </Grid>
</Window>
